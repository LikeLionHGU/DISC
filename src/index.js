import React from 'react';
import ReactDOM from 'react-dom/client';
import Appseoyean from './Appseoyean';

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(
  <React.StrictMode>
    <Appseoyean />
  </React.StrictMode>
);
